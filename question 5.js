/* 5. Create an array called fruits with at least 5 fruit names. Print:

* The whole array
* The number of items in the array */

const fruits = ["apple", "banana", "orange", "grape", "mango"];

console.log(fruits);

console.log("Number of items in the array:", fruits.length);